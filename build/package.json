{
  "name": "pm2-obplus-server",
  "scripts": {
    "start": "pm2 start ecosystem.config.js && pm2 logs",
    "start_test": "set NODE_ENV=production&& node ./dist/index.js",
    "stop": "pm2 stop all && pm2 kill",
    "reload": "pm2 reload all && pm2 status && pm2 logs",
    "restart": "pm2 restart all && pm2 logs",
    "stop_r": "pm2 stop OBPLUS",
    "stop_rs": "pm2 stop SEARCH",
    "restart_r": "pm2 restart OBPLUS && pm2 logs",
    "restart_rs": "pm2 restart SEARCH && pm2 logs",
    "logs": "pm2 logs",
    "logs_r": "pm2 logs OBPLUS",
    "logs_rs": "pm2 logs SEARCH",
    "status": "pm2 status",
    "monit": "pm2 monit"
  },
  "dependencies": {
    "pm2": "^6.0.8"
  }
}
